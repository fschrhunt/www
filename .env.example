# Server-only Resend key with sending access to the verified domain.
RESEND_API_KEY=
CONTACT_FROM="fschrhunt.com <contact@fschrhunt.com>"

# Contact attempts are counted by the RateLimiter Durable Object, which needs
# no configuration. Copy this file to .dev.vars for `npm run preview`.
