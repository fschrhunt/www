# Writing routes

`[slug]/page.tsx` renders the Markdown and MDX files in `src/content/writings/`.
See [the writing guide](../../content/README.md) for adding or editing a
note. Content files supply metadata and homepage entries automatically.
